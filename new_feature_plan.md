**sync**  sync profile, traninig session, category, group, when loggin using same account. 

**UI**  move the connectted number to right side of ble icon, small label , display as number

**feature**  on the top add training session filter, filter by category, group,


**page detail** about the session detail, make the card tight, like general info data duration type one column, and avg max min one column, remove calories.  Can HRV analysis have some visulization, and does HRV have acute specifications? like in a training session?

**setting page** move the signout button to right side, smaller this part. make categories and groups same row, change to droplist, organization&users if not useful, remove? move the help after the app version. make the app version number same row.